<ul class="nav justify-content-center sticky-top bg-white">
  <li class="nav-link h5" >관리페이지</li>
</ul>
<div class="container mt-4">
  <%= form_with(model: @category, local: true) do |form| %>
	  <% if @category.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

	      <ul>
	      <% @category.errors.full_messages.each do |message| %>
	        <li><%= message %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <div class="form-group">
	    <%= form.label :name, '카테고리 이름' %>
	    <%= form.text_field :name, id: :category_name , class: "form-control"%>
	  </div>

	  <div class="form-group">
	    <%= form.label :priority, '우선순위' %>
	    <%= form.collection_select :priority, Category.order(:priority)+[Category.new(priority: Category.count+1)], :id, :priority,{},{class: "form-control"} %>
	  </div>
	  
	  <%= form.submit "제출" , class: "btn btn-primary btn-block"%>
	<% end %>

</div>

