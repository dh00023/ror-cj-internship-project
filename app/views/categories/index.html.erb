<ul class="nav justify-content-center sticky-top">
  <li class="nav-link h5" >관리페이지</li>
</ul>

<div class="container">

  <table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">종류</th>
        <th scope="col">우선순위</th>
        <th scope="col"></th>
        <th scope="col"></th>
      </tr>
    </thead>

    <tbody>
      <% @categories.each_with_index do |category,index| %>
        <tr>
          <th scope="row"><%=index+1%></th>
          <td><%= category.name %></td>
          <td><%= category.priority %></td>
          <td><%= link_to '수정', edit_category_path(category) ,class: "btn btn-outline-dark btn-sm"%></td>
          <td><%= link_to '배너',  category_banners_path(category) ,class: "btn btn-outline-dark btn-sm"%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <!--<%= link_to '카테고리 생성하기', new_category_path %>-->
</div>
